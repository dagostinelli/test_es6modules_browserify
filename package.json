{
  "name": "test",
  "scripts": {
    "start": "rm -rf build && mkdir build && ./node_modules/.bin/browserify -t babelify src/index.js -o build/index.js && cp src/index.html build/index.html && gulp"
  },
  "devDependencies": {
    "babel": "^5.8.34",
    "babelify": "^6.4.0",
    "browserify": "^12.0.1",
    "gulp": "^3.9.0",
    "gulp-connect": "^2.2.0",
    "history": "^1.13.1"
  }
}
